[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300

[[cron]]
name = "weekly-digest"
schedule = "0 12 * * 1"
command = "python -m cron.weekly_digest"
