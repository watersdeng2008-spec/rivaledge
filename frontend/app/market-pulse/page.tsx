import MarketPulseDashboard from '../components/MarketPulseDashboard';

export const metadata = {
  title: 'Market Pulse — RivalEdge',
  description: 'Weekly market intelligence summary. Track competitor moves, AI visibility shifts, pricing changes, and emerging threats.',
};

export default function MarketPulsePage() {
  return <MarketPulseDashboard />;
}
